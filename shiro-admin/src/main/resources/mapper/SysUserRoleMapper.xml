<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tc.shiro.mapper.SysUserRoleMapper">
    <resultMap id="BaseResultMap" type="org.tc.shiro.po.SysUserRole">
        <!--
          WARNING - @mbg.generated
        -->
        <id column="id" jdbcType="TINYINT" property="id"/>
        <result column="user_id" jdbcType="TINYINT" property="userId"/>
        <result column="role_id" jdbcType="TINYINT" property="roleId"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
    </resultMap>

    <!-- 批量添加用户角色关联表数据 -->
    <insert id="insertBatch" parameterType="map">
        INSERT INTO `sys_user_role`(`user_id`,`role_id`) VALUES
        <foreach collection="roleIds" item="role_id" separator=",">
            (#{userId},#{role_id})
        </foreach>
    </insert>

    <delete id="deleteByUid" parameterType="byte">
        DELETE FROM sys_user_role where user_id = #{id}
    </delete>

    <!-- 批量删除 -->
    <delete id="deleteByUids" parameterType="list">
        DELETE FROM sys_user_role where user_id in
        <foreach close=")" collection="ids" item="id" open="(" separator=",">
            #{id}
        </foreach>
    </delete>

    <delete id="deleteByRid" parameterType="byte">
        DELETE FROM sys_user_role where role_id = #{id}
    </delete>

    <!-- 批量删除 -->
    <delete id="deleteByRids" parameterType="list">
        DELETE FROM sys_user_role where role_id in
        <foreach close=")" collection="ids" item="id" open="(" separator=",">
            #{id}
        </foreach>
    </delete>

</mapper>